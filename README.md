# GCP Cloud Run Shell Example

## Usage

Clone this repo, then use `make` as follows:

```bash
$ make
help                 Show this help
sql-create           Create a Cloud SQL instance
build                Build container image
deploy               Deploy container on Cloud Run
```
